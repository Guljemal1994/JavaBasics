package class30_Map_Intergace;

public class Notes {
/*
 * Map - is an object that stores grup of objects key+vaule=entry;
 * Value in the Map can be Duplicate but keys always must be unique;
 * 
 * 
 * Map is an Interface-Map has four implementation classes: 1.HashMap
 *                                                           2.LinkedHasMap;
 *                                                           3.TreeMap;
 *                                                        4.HashTable;(very similar HAshMap)
 *                                                           
 * to add value into map--> .put(k,V);
 * to get a value from a map--> .get(Key);     
 * to remove the value/Key--> .remove(key);
 * to replace value of key --> .replace(Key);
 * to verify is key/value ixist in the map-->contanskey or value
 * 
 *                                                      
 *if we want to get all keys?--> .keySet();
 *if we want to get all values?--> .values();
 *                                                      
 *                                                            	
 */

}
